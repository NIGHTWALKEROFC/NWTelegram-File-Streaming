# codemagic.yaml

workflows:
  android-release:
    name: Android Release Build
    max_build_duration: 60

    environment:
      flutter: stable
      # FIX: Gradle 8.13 + AGP 8.9 require Java 17 explicitly.
      # Without this, Codemagic may pick Java 11 and the build fails.
      java: 17
      groups:
        - telegram_keys

    triggering:
      events:
        - push
        - tag
      branch_patterns:
        - pattern: main
          include: true
          source: true

    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get

      - name: Analyze code
        script: |
          flutter analyze --no-fatal-infos || true

      - name: Build Android APK (release)
        script: |
          flutter build apk \
            --release \
            --split-per-abi \
            --dart-define=TG_API_ID=${TG_API_ID} \
            --dart-define=TG_API_HASH=${TG_API_HASH} \
            --build-name="1.0.0" \
            --build-number=$BUILD_NUMBER

      - name: Build Android App Bundle (AAB)
        script: |
          flutter build appbundle \
            --release \
            --dart-define=TG_API_ID=${TG_API_ID} \
            --dart-define=TG_API_HASH=${TG_API_HASH} \
            --build-name="1.0.0" \
            --build-number=$BUILD_NUMBER

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
      - build/app/outputs/bundle/release/*.aab
      - flutter_drive.log

    publishing:
      email:
        recipients:
          - your@email.com
        notify:
          success: true
          failure: true
